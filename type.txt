/* Typewriter */

.typewriter {
  font-size: 34px;
  font-family: "Courier New", monospace;
}

.cursor {
  display: inline-block;
  margin-left: 4px;
  font-weight: 340;
  font-size: 43px;            /* slightly bigger than text so it pops */
  line-height: 1;
  animation: cursor-blink 0.9s steps(2, start) infinite;
}

@keyframes cursor-blink {
  0%,
  50% {
    opacity: 1;
  }
  51%,
  100% {
    opacity: 0;
  }
}